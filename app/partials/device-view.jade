.device(ng-controller='DeviceController')
    h3 Device {{ device.id }}
        input.filter(type='text', ng-model="filter")
    .error(ng-hide="deviceAvailable") Device Unavailable
    .logcat
        logcat-entry(ng-repeat="entry in logcat", entry="entry")
